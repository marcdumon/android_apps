<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Light by default. values-night/themes.xml replaces this whole theme when the system
         is in dark mode, which is what makes the WebView report a dark app to the page. -->
    <style name="AppTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">#FFFFFF</item>
    </style>
</resources>
